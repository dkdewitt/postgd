import std.stdio;
import postid.connection;


void main() {

    /*auto conn = PQconnectdb("user=test password=test dbname=usda hostaddr=127.0.0.1 port=5432"); 
    


    if (PQstatus(conn) != CONNECTION_OK)
    {
        writeln("Connection to database failed");
        //CloseConn(conn);
    }

    PGresult* res = PQexec(conn, "select authors, title from public.data_src where datasrc_id like 'D1066 ';");

        ResultSet rs = ResultSet(res);
        rs.getHeaders();
        rs.getRows();

        writeln();
        writeln(rs.columnNames);

*/


}
